# Terminal steps for creating a Repositories
1. Open the terminal and make sure it is in the correct directory
2. Initialize repo, tell gitbash to reach terminal command: 'git init'
      - We have to do this steps every new projects
 # Connect online and local repositories
 1. Creat the online repo to coonect with go to github.com and click 'new repo'
 2. Copy git remote and origin command and paste in visual basic terminal and hit enter
      - This coomand links your local repo to the remote destination
      - Check this by running the coomand 'git remote'
      - 'git add .' add all files in the directory
      - to save a version, you can coomit locally